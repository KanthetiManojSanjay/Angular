import { RepeatedDirective } from './repeated.directive';

describe('RepeatedDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatedDirective();
    expect(directive).toBeTruthy();
  });
});
